const bomDia = (nome) => {
    return `Bom dia senhor ${nome}`;
}

const boaTarde = (nome) => {
    return `Boa tarde senhora(a) ${nome}`;
}

const boaNoite = (nome) => {
    return `Boa noite senhor(a) ${nome}';`
}

module.exports = {
    bomDia,
    boaTarde,
    boaNoite
}