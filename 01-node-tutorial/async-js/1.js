const name = "Wellington Minerva"; //  Declara uma string chamada nome




const makeGreeting = (name) => {
    return `Hello, my name is ${name}`
}

const greeting = makeGreeting(name); // Declara outra ...
console.log(greeting)